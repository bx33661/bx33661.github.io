---
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import { BentoGrid, BentoCard, ProjectCard, InfoCard } from '@/components/react/bento-grid'
import { Card3D } from '@/components/react/card-3d'
import AnimatedElement from '@/components/react/animated-element'
import { buttonVariants } from '@/components/ui/button'
import Layout from '@/layouts/Layout.astro'
import { Icon } from 'astro-icon/components'
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import { getAllProjects } from '@/lib/data-utils'

const currentUrl = Astro.url;
const allProjects = await getAllProjects();

// Define gradients to cycle through for projects
const gradients = [
  "from-[#E0F7FA] to-[#B2DFDB] dark:from-teal-900/30 dark:to-emerald-900/30 text-teal-900 dark:text-teal-100",
  "from-[#F3E5F5] to-[#E1BEE7] dark:from-purple-900/30 dark:to-violet-900/30 text-purple-900 dark:text-purple-100",
  "from-[#FFF3E0] to-[#FFE0B2] dark:from-orange-900/30 dark:to-amber-900/30 text-orange-900 dark:text-orange-100",
  "from-[#E8F5E9] to-[#C8E6C9] dark:from-green-900/30 dark:to-lime-900/30 text-green-900 dark:text-green-100",
  "from-[#FFEBEE] to-[#FFCDD2] dark:from-red-900/30 dark:to-pink-900/30 text-red-900 dark:text-red-100",
  "from-[#E3F2FD] to-[#BBDEFB] dark:from-blue-900/30 dark:to-cyan-900/30 text-blue-900 dark:text-blue-100",
];

const getIcon = (project: any) => {
  const tags = project.data.tags || [];
  if (tags.includes('python')) return '🐍';
  if (tags.includes('react') || tags.includes('vue') || tags.includes('nextjs')) return '⚛️';
  if (tags.includes('security') || tags.includes('web') || tags.includes('ctf')) return '🛡️';
  if (tags.includes('ai') || tags.includes('ml')) return '🤖';
  if (tags.includes('tool') || tags.includes('utils')) return '🛠️';
  return '💻';
};
---

<Layout canonicalUrl={currentUrl}>
  <PageHead slot="head" title="个人简历 - BX" description="BX 的个人介绍、项目与安全研究经历。" canonicalURL={currentUrl} />
  
  <div class="flex flex-col gap-y-12 px-4 py-8 sm:px-8 lg:px-16 max-w-7xl mx-auto">
    <Breadcrumbs items={[{ label: '个人简历', icon: 'lucide:user' }]} />
    
    <!-- Hero Section with 3D Card -->
    <AnimatedElement client:load animation="scaleIn" duration={0.8}>
      <Card3D client:load className="w-full" intensity={20}>
        <div class="bg-gradient-to-br from-background/80 to-muted/80 backdrop-blur-md p-8 md:p-12 rounded-3xl border border-white/10 shadow-2xl flex flex-col md:flex-row items-center gap-8 md:gap-12 text-center md:text-left relative overflow-hidden">
          <!-- Background decoration -->
          <div class="absolute top-0 right-0 -translate-y-1/4 translate-x-1/4 w-96 h-96 bg-primary/10 rounded-full blur-3xl pointer-events-none"></div>
          <div class="absolute bottom-0 left-0 translate-y-1/4 -translate-x-1/4 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl pointer-events-none"></div>

          <div class="relative group shrink-0" aria-label="个人头像">
             <div class="w-32 h-32 md:w-40 md:h-40 rounded-full overflow-hidden border-4 border-white/50 dark:border-white/10 shadow-xl group-hover:scale-105 transition-transform duration-500 ease-out">
               <img src="/static/blogo.jpg" alt="BX 头像" class="w-full h-full object-cover" loading="lazy" decoding="async" width="160" height="160" />
             </div>
             <div class="absolute inset-0 rounded-full ring-2 ring-white/20 ring-offset-4 ring-offset-transparent"></div>
          </div>
          
          <div class="flex-1 space-y-4 relative z-10">
            <h1 class="text-4xl md:text-6xl font-black tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-foreground to-foreground/70">
              BX
            </h1>
            <p class="text-xl md:text-2xl font-medium text-muted-foreground">
              网络安全研究员 | Web 安全 | AI 应用
            </p>
            <p class="text-lg text-muted-foreground/80 max-w-2xl leading-relaxed">
              专注于 Web 安全、AI 应用与攻防研究，持续分享实战经验与项目成果。
            </p>
             <div class="flex flex-wrap gap-3 justify-center md:justify-start pt-4">
               <a href="https://github.com/bx33661" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-black/5 dark:bg-white/10 hover:bg-black/10 dark:hover:bg-white/20 transition-all duration-300 backdrop-blur-sm border border-black/5 dark:border-white/5">
                 <Icon name="lucide:github" class="size-5" />
                 <span class="font-medium">bx33661</span>
               </a>
               <div class="inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-black/5 dark:bg-white/10 hover:bg-black/10 dark:hover:bg-white/20 transition-all duration-300 cursor-pointer group relative backdrop-blur-sm border border-black/5 dark:border-white/5">
                 <Icon name="lucide:message-circle" class="size-5" />
                 <span class="font-medium">bx33661</span>
                 <span class="absolute bottom-full left-1/2 -translate-x-1/2 mb-2 px-3 py-1.5 text-xs bg-foreground text-background rounded-lg shadow-lg opacity-0 group-hover:opacity-100 transition-all duration-300 whitespace-nowrap pointer-events-none translate-y-2 group-hover:translate-y-0">WeChat</span>
               </div>
             </div>
          </div>
        </div>
      </Card3D>
    </AnimatedElement>

    <!-- Bento Grid Section -->
    <section class="space-y-8">
       <AnimatedElement client:load animation="slideUp" delay={0.2}>
        <div class="flex items-center justify-between mb-8">
          <h2 class="text-3xl font-bold flex items-center gap-3">
            <Icon name="lucide:grid" class="size-8" />
            全景概览
          </h2>
          <span class="text-muted-foreground text-sm font-medium px-3 py-1 rounded-full bg-muted">Welcome to my world</span>
        </div>
        
        <BentoGrid className="auto-rows-[minmax(180px,auto)] gap-6">
          
          <!-- Education - Pastel Green/Emerald -->
          <InfoCard 
            client:visible
            title="教育经历" 
            subtitle="海南大学" 
            content="网络空间安全学院 · 信息安全专业"
            icon="🎓"
            span="md:col-span-2"
            gradient="bg-gradient-to-br from-[#E0F7FA] to-[#B2DFDB] dark:from-emerald-900/30 dark:to-teal-900/30 border-emerald-100 dark:border-emerald-800"
          />

          <!-- HnuSec - Pastel Red/Rose -->
          <InfoCard 
            client:visible
            title="HnuSec" 
            subtitle="核心成员" 
            content="海南大学网络安全战队，专注于CTF竞赛与实战攻防技术研究。"
            image="/static/HnuSec.jpg"
            span="md:col-span-1"
            gradient="bg-gradient-to-br from-[#FFEBEE] to-[#FFCDD2] dark:from-red-900/30 dark:to-pink-900/30 border-red-100 dark:border-red-800"
          />

          <!-- XMCVE - Pastel Purple -->
          <InfoCard 
            client:visible
            title="XMCVE" 
            subtitle="漏洞研究" 
            content="专注于通用漏洞挖掘与安全研究，致力于提升软件安全性。"
            icon="🐛"
            span="md:col-span-1"
            gradient="bg-gradient-to-br from-[#F3E5F5] to-[#E1BEE7] dark:from-purple-900/30 dark:to-violet-900/30 border-purple-100 dark:border-purple-800"
          />

          <!-- Fruit-Guardians - Pastel Indigo -->
          <InfoCard 
            client:visible
            title="Fruit-Guardians" 
            subtitle="开源社区" 
            content="专注于安全工具开发与自动化审计的开源组织，维护多个安全项目。"
            image="/static/fruit.png"
            span="md:col-span-1"
            gradient="bg-gradient-to-br from-[#E8EAF6] to-[#C5CAE9] dark:from-indigo-900/30 dark:to-blue-900/30 border-indigo-100 dark:border-indigo-800"
          />

          <!-- Project - Pastel Orange/Peach -->
           <a href="https://github.com/Fruit-Guardians/PureAutoCodeql" target="_blank" class="contents md:col-span-1 group">
             <div class="h-full w-full md:col-span-1">
               <ProjectCard
                 client:visible
                 title="PuraAutoCodeQL"
                 description="自动化 CodeQL 分析工具"
                 icon="🚀"
                 color="from-[#FFE0B2] to-[#FFCC80] dark:from-orange-900/30 dark:to-amber-900/30 text-orange-900 dark:text-orange-100"
                 span="h-full w-full"
               />
             </div>
           </a>
           
           <!-- Research Focus - Pastel Blue -->
           <BentoCard client:visible className="md:col-span-2 bg-gradient-to-br from-[#E3F2FD] to-[#BBDEFB] dark:from-blue-900/30 dark:to-cyan-900/30 border-blue-100 dark:border-blue-800">
             <div class="flex flex-col h-full">
               <div class="flex items-center gap-3 mb-4">
                 <div class="p-2 rounded-lg bg-white/50 dark:bg-black/20 backdrop-blur-sm">
                   <span class="text-2xl">🔬</span>
                 </div>
                 <h3 class="text-xl font-bold text-gray-800 dark:text-white">研究方向</h3>
               </div>
               <div class="flex flex-wrap gap-2">
                 {['Web 安全', 'AI 安全应用', '网络攻防', '漏洞挖掘'].map(tag => (
                   <span class="px-4 py-1.5 rounded-full bg-white/60 dark:bg-white/10 border border-white/20 text-sm font-semibold text-gray-700 dark:text-gray-200 shadow-sm">
                     {tag}
                   </span>
                 ))}
               </div>
               <p class="mt-6 text-sm font-medium text-gray-600 dark:text-gray-300 leading-relaxed">
                 致力于研究 Web 安全和探索新兴 AI-LLM 技术的应用与发展，以及前沿网络攻防技术的研究。
               </p>
             </div>
           </BentoCard>

           <!-- Bio / Motto - Modern Dark -->
           <BentoCard client:visible className="lg:col-span-4 md:col-span-2 flex items-center justify-center bg-[#263238] text-white min-h-[180px] relative overflow-hidden group">
              <!-- Decorative elements -->
              <div class="absolute top-0 right-0 w-64 h-64 bg-white/5 rounded-full -translate-y-1/2 translate-x-1/4 blur-3xl group-hover:bg-white/10 transition-colors duration-500"></div>
              <div class="absolute bottom-0 left-0 w-48 h-48 bg-white/5 rounded-full translate-y-1/2 -translate-x-1/4 blur-2xl group-hover:bg-white/10 transition-colors duration-500"></div>
              
              <div class="flex flex-col items-center text-center p-8 relative z-10">
                <Icon name="lucide:quote" class="size-10 text-white/20 mb-6" />
                <p class="text-3xl md:text-4xl font-serif italic tracking-wider text-white/90 leading-relaxed">
                  "一起见证星辰大海"
                </p>
              </div>
           </BentoCard>

        </BentoGrid>
      </AnimatedElement>
    </section>

    <!-- Other Projects Section -->
    <section class="space-y-6">
      <div class="flex items-center gap-3 mb-6">
        <h2 class="text-2xl font-bold flex items-center gap-2">
          <Icon name="lucide:folder-git-2" class="size-6" />
          更多项目
        </h2>
        <div class="h-px flex-1 bg-border/60"></div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {allProjects.map((project) => (
          <a 
            href={project.data.link || '#'} 
            target="_blank" 
            class="group relative flex flex-col p-5 rounded-xl border border-border/50 bg-card/50 hover:bg-card hover:border-border transition-all duration-300 hover:shadow-sm overflow-hidden"
          >
            <div class="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity text-4xl grayscale group-hover:grayscale-0">
              {getIcon(project)}
            </div>
            
            <div class="flex items-start justify-between mb-3 z-10">
              <div class="p-2 rounded-lg bg-primary/5 text-primary group-hover:bg-primary/10 transition-colors">
                <span class="text-xl">{getIcon(project)}</span>
              </div>
              {project.data.tags && (
                 <div class="flex gap-1 flex-wrap justify-end max-w-[50%]">
                  {project.data.tags.slice(0, 2).map(tag => (
                    <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-muted text-muted-foreground uppercase tracking-wider">
                      {tag}
                    </span>
                  ))}
                 </div>
              )}
            </div>

            <h3 class="font-bold text-lg mb-2 group-hover:text-primary transition-colors line-clamp-1 z-10">
              {project.data.name}
            </h3>
            
            <p class="text-sm text-muted-foreground line-clamp-2 mb-4 z-10">
              {project.data.description}
            </p>

            <div class="mt-auto flex items-center text-xs text-muted-foreground font-medium pt-3 border-t border-border/50 group-hover:border-border/80 transition-colors z-10">
              <span class="flex items-center gap-1 group-hover:text-foreground transition-colors">
                View Project <Icon name="lucide:arrow-up-right" class="size-3" />
              </span>
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- Footer Links -->
    <div class="flex justify-center pt-12 pb-8">
      <Link href="/" class={buttonVariants({ variant: 'ghost' }) + ' group text-muted-foreground hover:text-foreground transition-colors'}>
        <Icon name="lucide:arrow-left" class="mr-2 size-4 transition-transform group-hover:-translate-x-1" />
        返回首页
      </Link>
    </div>

  </div>
</Layout>
