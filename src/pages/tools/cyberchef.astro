---
const target = '/vendor/cyberchef/index.html'
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BX CyberChef</title>
    <link rel="icon" type="image/png" href="/apple-touch-icon.png" />
    <meta name="robots" content="noindex" />
    <style>
      html,
      body {
        width: 100%;
        height: 100%;
        margin: 0;
        padding: 0;
        background: #f5f5f5;
      }

      .shell {
        width: 100%;
        height: 100%;
      }

      .shell iframe {
        width: 100%;
        height: 100%;
        border: 0;
        display: block;
      }

      .fallback {
        max-width: 780px;
        margin: 12vh auto 0;
        padding: 16px;
        border: 1px solid #ccc;
        background: #fff;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      }

      .fallback[hidden] {
        display: none;
      }
    </style>
  </head>
  <body>
    <div class="shell" id="shell">
      <iframe id="cyberchef-frame" title="BX CyberChef" src={target}></iframe>
    </div>

    <div class="fallback" id="fallback" hidden>
      <p>CyberChef 资源尚未就绪。请先运行 <code>npm run cyberchef:sync</code>，然后刷新页面。</p>
      <p>你也可以直接访问：<a href={target}>/vendor/cyberchef/index.html</a></p>
    </div>

    <script is:inline define:vars={{ target }}>
      ;(() => {
        fetch(target, { method: 'HEAD', cache: 'no-store' })
          .then((response) => {
            if (response.ok) return
            const shell = document.getElementById('shell')
            const fallback = document.getElementById('fallback')
            if (shell) shell.remove()
            if (fallback) fallback.hidden = false
          })
          .catch(() => {
            const shell = document.getElementById('shell')
            const fallback = document.getElementById('fallback')
            if (shell) shell.remove()
            if (fallback) fallback.hidden = false
          })
      })()
    </script>
  </body>
</html>
