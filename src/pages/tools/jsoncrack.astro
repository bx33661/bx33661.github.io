---
const target = 'https://jsoncrack.com/editor'
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BX JSON Crack</title>
    <link rel="icon" type="image/png" href="/apple-touch-icon.png" />
    <meta name="robots" content="noindex" />
    <style>
      html,
      body {
        width: 100%;
        height: 100%;
        margin: 0;
        padding: 0;
        background: #f5f5f5;
      }

      iframe {
        width: 100%;
        height: 100%;
        border: 0;
        display: block;
      }

      .fallback {
        max-width: 760px;
        margin: 12vh auto 0;
        padding: 16px;
        border: 1px solid #ccc;
        background: #fff;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      }

      .fallback[hidden] {
        display: none;
      }
    </style>
  </head>
  <body>
    <iframe id="jsoncrack-frame" title="BX JSON Crack" src={target}></iframe>

    <div class="fallback" id="fallback" hidden>
      <p>JSON Crack 当前无法嵌入，可能是网络或第三方策略导致。</p>
      <p><a href={target} target="_blank" rel="noopener noreferrer">在新标签页打开 JSON Crack</a></p>
    </div>

    <script is:inline>
      ;(() => {
        const frame = document.getElementById('jsoncrack-frame')
        const fallback = document.getElementById('fallback')
        if (!frame || !fallback) return

        const timer = window.setTimeout(() => {
          fallback.hidden = false
        }, 6000)

        frame.addEventListener('load', () => {
          window.clearTimeout(timer)
        })
      })()
    </script>
  </body>
</html>
